<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
        android:name="com.example.sweet.SweetApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Sweet">


    <!-- MAIN / LAUNCHER -->
        <activity
            android:name=".MainActivity"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

        </activity>
        <!-- AUTH -->
        <activity android:name=".ui.LoginActivity" android:exported="false"/>
        <activity android:name=".ui.SignupActivity" android:exported="false"/>

        <!-- HOME -->
        <activity android:name=".ui.HomeActivity" android:exported="false"/>

        <!-- PROFILE -->
        <activity android:name=".ui.ProfileActivity" android:exported="false"/>
        <activity android:name=".ui.editprofile" android:exported="false"/>

        <!-- CART -->
        <activity android:name=".ui.CartActivity" android:exported="false"/>
        <activity android:name=".ui.item_cart" android:exported="false"/>
        <activity android:name=".ui.CheckoutActivity" android:exported="false"/>

        <!-- SWEETS -->
        <activity android:name=".ui.SweetDescActivity" android:exported="false"/>

        <!-- CATEGORIES -->
        <activity android:name=".ui.CategoriesList" android:exported="false"/>
        <activity android:name=".ui.Categories" android:exported="false"/>

        <!-- ADMIN -->
        <activity android:name=".ui.Admin_HomeActivity" android:exported="false"/>
        <activity android:name=".ui.AdminPanel" android:exported="false"/>
        <activity android:name=".ui.dialog_add_edit_sweet" android:exported="false"/>

    </application>


</manifest>